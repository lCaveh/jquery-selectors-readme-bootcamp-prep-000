function paragraphSelector() {
  const paragraph = $('p');
  return paragraph;
}
function lastImageSelector() {
  const lastImage = $('img:last');
  return lastImage;
}
function lastImageSelector() {
  const lastImage = $('img:last');
  return lastImage;
}
